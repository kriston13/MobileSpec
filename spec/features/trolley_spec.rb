require 'spec_helper'

describe "The trolley", :type => :feature, :js => true do


	it "requires a member to have stuff added to it" do
		pending "steps to be filled in"
	end
	
	it "requires a member on a local catalog to have stuff added to it" do
		pending "steps to be filled in"
	end
	
	
	it "can have one item added to it from the search results page" do
	
		pending "steps to be filled in"
	
	end
	
	it "can have two of the same item added to it from the search results page" do
		pending "steps to be filled in"
	end
	
	it "can have multiple items added into it from the search results page" do
		pending "steps to be filled in"
	
	end
	
	
	it "can have one item added to it from the subcategory product list page" do
		pending "steps to be filled in"
	end

	it "can have two of the same item added to it from the subcategory product list page" do
		pending "steps to be filled in"
	end
	
	it "can have an item added to it from the product details page" do
		pending "steps to be filled in"
	end
	
	it "can have multiple quantities of an item added to it from the product details page" do
		pending "steps to be filled in"
	end

	it "will not let you add more than the redemption limit" do
		pending "steps to be filled in"
	end

	it "will not let you add products that are out of stock" do
		pending "steps to be filled in"
	end
	
	
end